import { Header } from "@/components";
import EmailLoginContainer from "@/containers/auth/login/email";

const EmailLoginPage = () => {
  return (
    <>
      <EmailLoginContainer />
    </>
  );
};

export default EmailLoginPage;
